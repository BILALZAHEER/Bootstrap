<%if user_signed_in? %>
	<%= render 'form', student: @student %>
	<div class="container" style="margin-left: 42%">
	  <div>
	    <%= link_to 'Edit', @student, class: 'btn btn-primary btn-block' , style: 'width:10%' %>
	  </div>
	  <br>
	  <div>
	    <%= link_to 'Back', students_path , class: 'btn btn-primary btn-block' , style: 'width:10%' %>
	  </div>
	</div>
<%end%>